    for (let index = 0; index < msj.length; index++) {
        var letra = msj.charAt(index);
        if(isMayus(letra))
        {
            alert("La letra " + letraActual + " es mayúscula");
        }else{
            const textoEncriptado = Encriptar(msj.value)
            act_msj.value = textoEncriptado
            msj.value = ""
            act_msj.style.backgroundImage = "none"
        }        
    }